// kindred — `.` surface. K-1 ships the relationship model types + re-exported nostr-tools aliases.
// Model ops (K-2), backup (K-2), and the subpath surfaces (./handshake, ./bond, ./ken,
// ./discovery, ./invite) land in later tasks.
export * from './types.js'
